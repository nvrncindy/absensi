# Neon Postgres connection string
DATABASE_URL=

# Long random secret that gates the admin tab (office location, employee
# CRUD, face registration). Generate one with: openssl rand -hex 32
ADMIN_SECRET=
